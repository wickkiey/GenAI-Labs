version: '3.8'

services:
  crewai:
    build: .
    environment:
      - OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
      - OLLAMA_MODEL=qwen3:8b
    volumes:
      - .:/app
    networks:
      - default

networks:
  default:
    driver: bridge
